# Security policy

Please report suspected vulnerabilities privately through GitHub's private vulnerability reporting feature. Do not include real participant data, active tokens, or production credentials in a report; use redacted or synthetic reproductions.

Woodshed is currently pre-release. Supported-version details will be published with the first product release. Security-sensitive changes must fail closed, preserve phase-aware failure evidence without sensitive values, and bind release and cleanup operations to exact identities.
